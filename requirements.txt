# EL - Core Dependencies
python-telegram-bot>=21.0
openai-whisper>=20231117
numpy>=1.24
yt-dlp>=2024.0
youtube-transcript-api>=0.6.0

# Voice - install one or both
# pip install bark           # Human-like TTS (needs GPU)
# pip install piper-tts      # Lighter TTS alternative

# System requirement: ffmpeg (for voice message conversion)
# Ubuntu/Debian: sudo apt install ffmpeg
# macOS: brew install ffmpeg
